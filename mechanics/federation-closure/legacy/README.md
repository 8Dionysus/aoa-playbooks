# Federation Closure Legacy

Package-local legacy for `mechanics/federation-closure/`.

Use this only for old-path lookup. Current federation-closure behavior lives in
`README.md`, `PARTS.md`, and `parts/federation-surfaces/`.
