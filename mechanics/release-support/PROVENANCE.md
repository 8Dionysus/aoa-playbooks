# Release Support Provenance

## Active-first rule

Start from package-local release-support docs plus root release entrypoints.

## Center or local origin

Release-support is head-fed as publication posture and local as repo release
validation. Runtime deployment and CI execution stay with stronger owners.

## Previous placement

Mechanic payload moved into package-local parts on 2026-05-31.

The active decision is package-local for release-support payloads.
`docs/RELEASING.md` and `scripts/release_check.py` remain accepted-input
operator routes.

## Legacy boundary

Former root release-support doc/schema/example names are accepted-input legacy
names. The active payload lives under `mechanics/release-support/parts/`.
