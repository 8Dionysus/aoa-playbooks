# Scenario Composition Legacy

Package-local legacy for `mechanics/scenario-composition/`.

Use this only for old-path lookup. Current scenario-composition behavior lives
in `README.md`, `PARTS.md`, and `parts/composition-surfaces/`.
