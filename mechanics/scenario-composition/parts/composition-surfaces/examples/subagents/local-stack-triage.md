# Local Stack Triage

Playbook: `local-stack-diagnosis`

Use when both startup docs and runtime state look suspicious and you want one bounded split.

Expected split:

- canon scout returns authoritative startup files
- bringup operator returns a bounded startup attempt and blocker list
