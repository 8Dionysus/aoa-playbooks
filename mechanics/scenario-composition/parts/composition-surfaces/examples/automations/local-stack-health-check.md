# Local Stack Health Check

Playbook: `local-stack-diagnosis`

Use when you want a bounded local bring-up pass before a heavy coding session.

Suggested seed:

- locate canon through `mode.knowledge.authority-map`
- run the smallest viable bring-up through `workflow.operations.local-stack-bringup`
- if blocked, close with `mode.knowledge.sanitized-share`
