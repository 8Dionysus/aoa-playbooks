# Activation Legacy

Package-local legacy for `mechanics/activation/`.

Use this only for old-path lookup. Current activation behavior lives in
`README.md`, `PARTS.md`, and `parts/activation-surface/`.
