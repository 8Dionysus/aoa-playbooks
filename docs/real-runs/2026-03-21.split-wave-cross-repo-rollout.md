# split-wave-cross-repo-rollout reviewed summary

## Run Header

- Run identifier: `2026-03-21 section-expand wave`
- Date: `2026-03-21`
- Owning playbook: `AOA-P-0017 split-wave-cross-repo-rollout`

## Entry Signal

- `aoa-routing` needed to add `expand` without copying section payloads into routing outputs.
- That downstream routing change depended on source-owned full-section surfaces landing first in `aoa-techniques`, `aoa-skills`, and `aoa-evals`, so the route required ordered multi-wave sequencing rather than a single generic rollout.

## Boundary Summary

- Upstream source-owned boundaries in scope: `aoa-techniques`, `aoa-skills`, `aoa-evals`
- Downstream consumer boundary in scope: `aoa-routing`
- `AOA-P-0010` was ruled out because the downstream routing merge explicitly depended on upstream source-owned merges and reran downstream validation after those upstream surfaces existed on `main`.
- `AOA-P-0018` was ruled out because the route was not failure-driven remediation.
- `AOA-P-0019` was ruled out because there was no named cutover window or authority switch.
- `AOA-P-0020` was ruled out because there was no live incident stabilization or recovery handoff.

## Required Artifacts

- `wave_plan`: reciprocal merge notes set the order. `aoa-techniques#99`, `aoa-skills#48`, and `aoa-evals#10` each say to merge before the routing expand-dispatch PR, while `aoa-routing#6` says to merge after the three source-repo section PRs.
- `bridge_surface_pack`: the three upstream PRs each landed a source-owned full section surface: `generated/technique_sections.full.json`, `generated/skill_sections.full.json`, and `generated/eval_sections.full.json`.
- `downstream_revalidation_pack`: `aoa-routing#6` rebuilt and validated router outputs against worktrees rooted at the three upstream repos before merge.
- `handoff_record`: the upstream merge notes handed the route forward to the routing expand-dispatch PR, and the routing PR closed the same dependency chain in its own merge notes.

## Closure Class

- `merge`
- The route closed when `aoa-routing#6` merged after the three upstream bridge PRs were already on `main`.

## Follow-On Route

- No residual governed follow-on route was recorded at closure.
- Later routing expansion work returned to ordinary repo-owned change routing rather than staying inside this ordered-wave run.

## Composition Signals

- `new failure/follow-up mapping not already covered`: the case shows a real ordered dependency between source-owned bridge surfaces and downstream router expansion, but it does not yet establish a stable failure or follow-up mapping beyond what `AOA-P-0010` and existing rollout doctrine already cover.
- `handoff bridge candidate`: a possible handoff bridge exists around reciprocal merge-note sequencing between source repos and routing, but one run is too weak to promote it.
- `subagent split candidate`: no stable split surfaced from this run alone.
- `automation seed candidate`: a future seed to check upstream bridge readiness before downstream router merge is plausible, but this single run does not prove it is stable.
- `insufficient signal`: yes. The first reviewed run is qualifying evidence for the playbook boundary, not yet sufficient evidence for composition promotion.

## Residual Risk

- Future ordered-wave routes of the same family could still collapse back into generic rollout language if merge-order evidence and downstream revalidation surfaces are not explicit.
- A second independent ordered-wave case is still needed to separate stable split-wave adjuncts from one-off section-expand details.

## Evidence Links

- [aoa-techniques#99](https://github.com/8Dionysus/aoa-techniques/pull/99) upstream bridge surface plus the merge-before note for the routing expand-dispatch PR
- [aoa-skills#48](https://github.com/8Dionysus/aoa-skills/pull/48) upstream bridge surface plus the merge-before note for the routing expand-dispatch PR
- [aoa-evals#10](https://github.com/8Dionysus/aoa-evals/pull/10) upstream bridge surface plus the merge-before note for the routing expand-dispatch PR
- [aoa-routing#6](https://github.com/8Dionysus/aoa-routing/pull/6) downstream revalidation against the three source repos plus the merge-after note and route closure
