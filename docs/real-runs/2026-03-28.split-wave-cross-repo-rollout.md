# split-wave-cross-repo-rollout reviewed summary

## Run Header

- Run identifier: `2026-03-28 wave-9 tiny-router bridge`
- Date: `2026-03-28`
- Owning playbook: `AOA-P-0017 split-wave-cross-repo-rollout`

## Entry Signal

- `aoa-routing` needed to add the router-owned half of wave-9 as a two-stage seam without copying skill meaning into routing.
- That downstream routing seam depended on `aoa-skills` publishing tiny-router bridge outputs first, so the route required an ordered multi-wave rollout rather than a single generic cross-repo merge.

## Boundary Summary

- Upstream source-owned boundary in scope: `aoa-skills`
- Downstream consumer boundary in scope: `aoa-routing`
- This is a different-family ordered-wave case than the March 21 section-expand summary: the bridge artifacts here are tiny-router compression and bridge outputs, not source-owned section payload surfaces.
- `AOA-P-0010` was ruled out because the downstream router seam explicitly consumed sibling bridge outputs and the closure notes recorded a split-wave merge-order constraint for waves that consume sibling repo surfaces from `main`.
- `AOA-P-0018` was ruled out because the route was not failure-driven remediation.
- `AOA-P-0019` was ruled out because there was no named cutover window, freeze posture, or authority switch.
- `AOA-P-0020` was ruled out because there was no live incident stabilization or recovery handoff.

## Required Artifacts

- `wave_plan`: wave-9 was published as a producer-to-consumer split wave. `aoa-skills#83` landed the bridge outputs, `aoa-routing#32` consumed them as the router-owned adjacent seam, and `aoa-routing#33` recorded the merged milestone plus the split-wave merge-order constraint.
- `bridge_surface_pack`: `aoa-skills#83` landed the skill-derived tiny-router bridge outputs, including `generated/tiny_router_candidate_bands.json`, `generated/tiny_router_capsules.min.json`, and `generated/tiny_router_skill_signals.json`.
- `downstream_revalidation_pack`: `aoa-routing#32` rebuilt and validated the two-stage router against `../aoa-skills` through `python scripts/build_two_stage_skill_router.py --routing-root . --skills-root ../aoa-skills --check` and `python scripts/validate_two_stage_skill_router.py --routing-root . --skills-root ../aoa-skills --pretty`.
- `handoff_record`: `aoa-routing#32` landed `docs/CROSS_REPO_ROUTER_BRIDGE.md` as an explicit cross-repo bridge surface, and `aoa-routing#33` recorded the merged wave-9 milestone and its split-wave merge-order constraint.

## Closure Class

- `merge`
- The route closed when `aoa-routing#32` merged after the upstream bridge outputs were already available from `aoa-skills#83`, with `aoa-routing#33` then recording the merged milestone and boundary.

## Follow-On Route

- No residual governed follow-on route was recorded at closure.
- The next repo-first follow-on is composition review inside `aoa-playbooks`, not another documented operational wave from the source repos.

## Composition Signals

- `new failure/follow-up mapping not already covered`: yes. Across this run and the March 21 section-expand wave, a stable split-wave mapping is now visible: when routing consumes sibling repo bridge surfaces from `main`, merge-order constraints and downstream revalidation must stay explicit rather than collapsing into generic `AOA-P-0010` rollout language.
- `handoff bridge candidate`: yes. This run adds an explicit `docs/CROSS_REPO_ROUTER_BRIDGE.md` consumer bridge surface, and together with the March 21 reciprocal merge-note pattern it supports a stable split-wave handoff bridge candidate for producer-to-routing waves.
- `subagent split candidate`: no stable split is explicit yet.
- `automation seed candidate`: possible, but still secondary to the clearer handoff bridge candidate.
- `insufficient signal`: no. The second different-family reviewed run is enough to open later composition review, even though it does not justify direct composition promotion by itself.

## Residual Risk

- The two reviewed runs are both routing-adjacent, so composition review still needs to check that any new adjunct is truly playbook-owned and not overfit to routing-specific naming.
- A later non-routing split-wave case would still improve confidence before any direct composition change lands.

## Evidence Links

- [aoa-skills#83](https://github.com/8Dionysus/aoa-skills/pull/83) upstream tiny-router bridge outputs and wave-9 producer surface
- [aoa-routing#32](https://github.com/8Dionysus/aoa-routing/pull/32) downstream two-stage seam, downstream revalidation against `../aoa-skills`, and explicit `docs/CROSS_REPO_ROUTER_BRIDGE.md`
- [aoa-routing#33](https://github.com/8Dionysus/aoa-routing/pull/33) merged milestone and split-wave merge-order constraint for sibling-surface consumer waves
